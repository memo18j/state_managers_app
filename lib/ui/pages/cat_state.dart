import 'package:flutter/material.dart';

class CatSatePage extends StatelessWidget {
  const CatSatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
